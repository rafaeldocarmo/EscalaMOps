/**
 * Disparado no cliente após persistir a equipe selecionada (cookie) no header.
 * Componentes que dependem do escopo de equipe devem escutar e refetch.
 */
export const SELECTED_TEAM_CHANGED_EVENT = "mops-selected-team-changed";
